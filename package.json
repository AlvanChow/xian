{
  "name": "valuegrid",
  "version": "1.0.0",
  "description": "ValueGrid — an interactive, canvas-rendered world map of global capital flows with full provenance tagging.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.mjs",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "playwright test",
    "test:unit": "node --test \"tests/unit/*.test.mjs\""
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "eslint": "^9.13.0",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
